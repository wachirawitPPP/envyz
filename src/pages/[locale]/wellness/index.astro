---
import Header from "~/components/widgets/Header.astro";
import PageLayout from "~/layouts/PageLayout.astro";
import { getTranslations, t } from "../../../i18n";
import Advertise1 from "~/components/widgets/Advertise1.astro";
import ProductList from "~/components/widgets/ProductList.jsx";
const { locale } = Astro.params;
console.log(Astro.url.pathname)
const metadata = {
  title: 'AstroWind — Free template for creating websites with Astro + Tailwind CSS',
  ignoreTitleTemplate: true,
};

export function getStaticPaths() {
  return [
    { params: { locale: "en" } },
    { params: { locale: "fr" } },
    { params: { locale: "th" } },
    { params: { locale: "la" } },
  ];
}
---
<PageLayout  metadata={metadata} locale={locale}>
 
    <Advertise1/>
    <ProductList header="ความงาม" subHeader='' locale={locale}  client:load >
  
</PageLayout>

